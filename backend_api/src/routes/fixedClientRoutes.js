import { Router } from 'express'; import * as c from '../controllers/fixedClientController.js'; import { requireAuth,requireRoles } from '../middleware/authMiddleware.js'; const router=Router(); router.use(requireAuth); router.get('/fixed-clients',c.list); router.post('/fixed-clients',requireRoles('admin','gerente'),c.create); router.patch('/fixed-clients/payments/:id',requireRoles('admin','gerente'),c.pay); router.delete('/fixed-clients/:code',requireRoles('admin','gerente'),c.cancel); export default router;
